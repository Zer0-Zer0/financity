using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class bancocobrar : MonoBehaviour
{

//    public Calendario dias; após conclusão do script
public void cobrar(string metodo)
{
    if (metodo == "À vista")
    {
            //como fazer pra cobrar a vista aq dps
    } else if (metodo == "Parcelado simples")
    {
        // como fazer para distribuir custos fixos ou variáveis, o simples terá menor prazo e menos juros
    } else if (metodo == "Parcelado composto")
    {
        //metodo para o composto
    }
}
}
